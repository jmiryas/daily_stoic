import 'package:flutter/material.dart';

class AllStoicScreen extends StatelessWidget {
  const AllStoicScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
